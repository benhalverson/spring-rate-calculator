## Feature flags
# When true, uses the hybrid storage backend that queues local writes and attempts cloud sync.
VITE_ENABLE_CLOUD_SYNC=false

## Cloud sync
# Optional URL that receives queued operations as JSON via POST.
# Example: https://example.com/api/spring-rate/sync
VITE_CLOUD_SYNC_URL=

